// Hello human.
// Welcome to WWS project.
// This readme compiles to index.html.
// Created by humans before AI coding era.
// Maintained by humans.
// For humans.
// We're just getting started.
// A hui hou!
// - Breck

permalink index.html
openGraphImage og.png
title World Wide Scroll - successor to web that works offline

header.scroll

css
 center > .scrollParagraph:empty { display: none; }
 .iframeHolder { height: 148px; }

# World Wide Scroll
 style font-weight: 600; font-size: clamp(2.25rem, 6vw, 3.5rem); letter-spacing: -0.045em; max-width: 1000px; margin: 0 auto 0.5rem;

container 880px

## Works offline. Simpler. Public domain.
 style font-weight: 400; font-size: clamp(1.2rem, 3vw, 1.5rem); color: #65716b; max-width: 680px; margin: 0 auto 1rem;

# Downloading the Scroll *is free*. Install now: `npm install -g breck7/wws`.
 style font-weight: 400; font-size: 1.15rem; margin: 0.75rem auto; max-width: 720px;
center
root.tsv
 limit 7
  where tags includes featured
   compose tag <div class="iframeHolder"><div><a href="{web}">~{name}</a></div><iframe src="{web}" frameborder="0" allow="geolocation 'none'"></iframe></div>
    printColumn tag

# Register your Scroll name with a patch.
 style font-weight: 400; font-size: 1.15rem; margin: 0.75rem auto; max-width: 720px;
 https://github.com/breck7/wws/blob/main/root.scroll patch

scrollVersionLink
